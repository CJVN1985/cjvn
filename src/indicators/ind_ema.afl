// Example EMA indicator (placeholder)
SetBarsRequired(10000, 0);

ind_ema_per = Nz(Param("EMA Period", 20, 2, 200, 1), 20);
ind_ema     = EMA(Close, ind_ema_per);

Plot(ind_ema, "ind_ema", colorDefault, styleLine);
